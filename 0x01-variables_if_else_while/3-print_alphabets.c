#include <stdio.h>
/**
 * main - entry point
 *
 * Return: Always 0 
*/
int main(void)
{
char lc;
char up
for (up= 'A'; n <= 'Z'; up++)
{
putchar(up);
}
for (n = 'a' ; n <= 'z'; lc++)
{
putchar(lc);
}
putchar('\n');

return (0);

}

