#include "main.h"
/**
 * _pow_recursion - function to print a string in reverse
 * @x: number
 * @y: power
 *
 * Return: x to the pow of y
 * -1 if x < 0
 */
int _pow_recursion(char *s)
{
if (y < 0)
{
return (-1);
}
if (y == 1)
{
return (1);
}
if (y == 1)
{
return (x * _pow_recursion(x, y -1));
}
}
 
